// PLACEHOLDER — being written now (queue items 3 + 4a). Absorbs the addon's `DATA.md`.
import type { Section } from "./types.ts";

export const emberSection: Section = {
    id: "ember",
    title: "Using the grid in Ember",
    blocks: [{ kind: "p", text: "Coming from `DATA.md` — being written." }],
};
