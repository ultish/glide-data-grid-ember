// PLACEHOLDER — being written now (queue item 4b). Absorbs the reference half of `THEMING.md`.
import type { Section } from "./types.ts";

export const themeReferenceSection: Section = {
    id: "theme-reference",
    title: "Theme reference",
    blocks: [{ kind: "p", text: "Coming from `THEMING.md` — being written." }],
};
